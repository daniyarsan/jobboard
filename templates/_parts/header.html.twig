<header class="header-area">
    <div class="header-top">
        <div class="container">
            <div class="row">
                <div class="col-lg-2">
                    <div class="logo-area">
                        <a href="index.html"><img src="assets/images/logo.png" alt="logo"></a>
                    </div>
                </div>
                <div class="col-lg-10">
                    <div class="custom-navbar">
                        <span></span>
                        <span></span>
                        <span></span>
                    </div>
                    <div class="main-menu">
                        <ul>
                            <li>
                                <a href="{{ path('jobs_index') }}">Jobs</a>
                            </li>
                            <li>
                                <a href="{{ path('companies_index') }}">Companies</a>
                            </li>
                            <li>
                                <a href="{{ path('profiles_index') }}">Profiles</a>
                            </li>

                            {% if app.user %}
                                <li class="menu-btn dropdown">
                                    <a class="dropdown-toggle" href="{{ path('my_account_index') }}" id="myaccount" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                        My Account
                                    </a>
                                    <div class="dropdown-menu" aria-labelledby="myaccount">
                                        <a class="dropdown-item" href="{{ path('my_account_my_jobs') }}">My Jobs</a>
                                        <a class="dropdown-item" href="{{ path('my_account_profile') }}">Profile</a>
                                        <a class="dropdown-item" href="{{ path('security_logout') }}">Logout</a>
                                    </div>
                                </li>
                            {% else %}
                                <li class="menu-btn">
                                    <a href="{{ path('security_login') }}" class="login">Login</a>
                                    <a href="{{ path('security_registration') }}" class="template-btn">Register</a>
                                </li>
                            {% endif %}
                        </ul>
                    </div>
                </div>
            </div>
        </div>
    </div>
</header>


